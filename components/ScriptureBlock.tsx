import React from 'react';

// This component is no longer used as the "Base Text" section has been removed from the application.
const ScriptureBlock: React.FC = () => null;

export { ScriptureBlock };
